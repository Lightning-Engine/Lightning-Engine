#ifndef LI_GEOMETRY_H
#define LI_GEOMETRY_H

#include "mesh.hh"
#include <initializer_list>

namespace li {
	namespace geo {

		template<typename T>
		void _sort_updown_leftright(std::vector<li::vector<T, 2>>& points) {
		
		}

		template<typename T>
		mesh<li::vector<T, 2>> triangulate(std::initializer_list<li::vector<T, 2>> points) {
			mesh<li::vector<T, 2> result;

		}

	}
}

#endif